{{ form_start(form) }}
<div class="my-custom-class-for-errors">
    {{ form_errors(form) }}
</div>
<div class="row">
    <div class="col">
    {{ form_widget(form) }}
    </div>
</div>
<input type="submit" value="Verstuur" class="btn btn-dark">
{{ form_end(form) }}
